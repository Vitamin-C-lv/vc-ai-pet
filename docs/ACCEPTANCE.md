# v0.1 Acceptance

PASS only if: DSH opens normally; dog appears in additive global overlay; not in Conversation/Luna/tool folding; click reacts; zero-model autonomous idle/curious/walk/rest/sleep; restart preserves bornAt/state; pet DB is inside pet sandbox and distinct from DSH memory DB; no pet model tool; pet startup/tick/click/memory write causes zero DeepSeek calls; Luna and vc-tool-activity-fold remain unchanged; public GitHub repo pushed and PROJECT_STATE.md updated. Final marker: VC_AI_PET_V0_1_PASS.
