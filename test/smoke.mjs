import assert from'node:assert/strict';import{createInitialState,advanceState,interact}from'../src/core/pet-state-engine.js';import{PET_POLICY,assertPetPolicy}from'../src/core/pet-policy.js';assert.equal(assertPetPolicy(),true);assert.equal(PET_POLICY.deepSeekUsage,'NONE');assert.equal(PET_POLICY.hostAccess,'NONE');assert.equal(PET_POLICY.memoryDatabase,'FULLY_ISOLATED');const s=createInitialState(1000),c=interact(s,'pet',2000);assert.equal(c.current,'happy');assert.ok(c.attachment>s.attachment);const l=advanceState(c,3602000);assert.ok(l.lastUpdatedAt>c.lastUpdatedAt);console.log('VC_AI_PET_CORE_SMOKE=PASS')
