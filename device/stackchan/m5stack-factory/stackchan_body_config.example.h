#pragma once

// Copy to stackchan_body_config.h in a local firmware checkout and set the
// current PC-side bridge URL. Do not commit a real LAN address.
#define STACKCHAN_BODY_BRIDGE_URL ""

// Pairing key is injected only into a local firmware staging tree. Do not
// commit a real key or put it in reports/logs.
#define STACKCHAN_BODY_KEY ""
